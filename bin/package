#!/bin/bash
# set -Eeuo pipefail
set -x

rm application.zip

zip -r application.zip \
    bin \
    sql \
    static \
    templates \
    .env \
    application.py \
    config.py \
    Procfile \
    requirements.txt \