#!/bin/bash
set -Eeuo pipefail
set -x

./bin/manage_db create
export FLASK_ENV=production
export FLASK_APP=application

uwsgi --http :5000 --wsgi-file application.py --master --processes 4 --threads 2
